import { Meta } from '@storybook/addon-docs'

<Meta title="Helpers/Media Queries" />

# Media Queries

```jsx
import { media } from 'roots-design-system'
```

<table>
  <thead style={{ backgroundColor: '#d4d4d4' }}>
    <tr>
      <th>Name</th>
      <th>Variable</th>
      <th>Value</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Mobile</td>
      <td>small</td>
      <td>320px</td>
    </tr>
    <tr>
      <td>Tablet</td>
      <td>medium</td>
      <td>672px</td>
    </tr>
    <tr>
      <td>Desktop</td>
      <td>large</td>
      <td>1056px</td>
    </tr>
    <tr>
      <td>Large Desktop</td>
      <td>xlarge</td>
      <td>1312px</td>
    </tr>
    <tr>
      <td>Larger Desktop</td>
      <td>xxlarge</td>
      <td>1584px</td>
    </tr>
  </tbody>
</table>

## Uso

```js
  ${media.lessThan('small')`
   ...code
 `}

  ${media.greaterThan('small')`
   ...code
 `}
```

## Exemplo base com media queries

```css
import styled from 'styled-components';

import { media } from 'roots-design-system';

export const Wrapper = styled.div`
  ${({ theme }) => css`
    display: flex;
    background-color: ${theme.colors.primary};
    color: ${theme.colors.white};

    ${media.lessThan('small')`
      width: 100%;
    `}
  `}
`;
```
